/**
 * SEU TREINO AQUI — lógica da tela inicial pós-login (dashboard.html).
 *
 * Elementos do HTML:
 * - #user-greeting   (span onde deve aparecer o email do usuário)
 * - #logout-button   (botão de sair)
 *
 * Passos sugeridos:
 * 1. Ao carregar a página, pegar o usuário salvo com Api.getUser()
 *    e mostrar algo como "Olá, fulano@email.com" no #user-greeting.
 * 2. Escutar o clique do #logout-button:
 *    - chamar Api.logout()
 *    - redirecionar para 'index.html'
 */

document.addEventListener('DOMContentLoaded', () => {
  // seu código aqui
});
