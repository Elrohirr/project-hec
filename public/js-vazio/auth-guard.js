/**
 * SEU TREINO AQUI.
 * Este script é incluído em toda página que exige login (ex: dashboard.html).
 *
 * TODO: se Api.isAuthenticated() for falso, redirecionar para 'index.html'
 * (window.location.href = 'index.html').
 *
 * Dica: envolva isso numa IIFE (função que roda sozinha) para que a
 * checagem aconteça assim que o script for carregado, sem precisar
 * esperar nenhum evento.
 */
